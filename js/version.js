/*
 * Devoir Numérique - Version applicative
 * Source unique de vérité : consommée par index.html (affichage)
 * et par sw.js via importScripts (CACHE_NAME).
 * À incrémenter à chaque déploiement modifiant data/, JS ou CSS.
 */
(function (scope) {
    'use strict';
    scope.APP_VERSION = '4.28.0';
})(typeof self !== 'undefined' ? self : window);
